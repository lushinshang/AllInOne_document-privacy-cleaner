# All-in-One 文件隱私清理器：使用者操作說明

- 適用對象：需要把圖片、PDF 或 Office 文件交付給外部人員的一般使用者
- 使用方式：直接用瀏覽器開啟單一 `index.html`
- 最後更新：2026-08-28

## 先用一句話了解這個工具

檔案看起來只有照片、文字或表格，裡面仍可能藏有拍攝地點、作者姓名、公司名稱、註解、講者備忘稿或本機檔案路徑等資訊。這個工具會先找出這些「藏在檔案裡的附加資料（Metadata）」，再另外產生一份清理版供你下載。

所有處理都在你的瀏覽器內完成，不會把文件上傳到伺服器，也不會覆寫電腦裡的原始檔。

## 一分鐘快速操作

1. 進入 `AllInOne_document-privacy-cleaner` 資料夾。
2. 雙擊正式版 `index.html`，用 Chrome、Edge 或其他現代瀏覽器開啟。
3. 把檔案拖進「拖放多個檔案至此」區域，或按「選取檔案」。
4. 查看每份文件的風險顏色與掃描結果。
5. 不想處理的文件，取消勾選「此份要清除」。
6. 按「開始清除 Metadata」。
7. 確認「清除後」欄位顯示「✓ 已清除」，再下載單檔清除版或批次 ZIP。

建議先用副本測試，下載後再開啟清理版，確認圖片、文字、表格、投影片及連結等重要內容符合預期。

## 可以處理哪些檔案？

| 類型 | 支援副檔名 | 單檔上限 |
|---|---|---:|
| 圖片 | `.jpg`、`.jpeg`、`.png`、`.webp` | 20 MB |
| PDF | `.pdf` | 50 MB |
| Word | `.docx` | 20 MB |
| Excel | `.xlsx` | 20 MB |
| PowerPoint | `.pptx` | 20 MB |

一次加入的全部檔案合計不可超過 100 MB。超過限制時，工具會在讀取檔案內容前拒絕加入。

舊版 Word、Excel、PowerPoint 格式（`.doc`、`.xls`、`.ppt`）、壓縮檔、文字檔及其他副檔名目前不支援。

## 完整操作步驟

### 1. 開啟正確的檔案

請開啟產品根目錄裡的 `index.html`，不要開啟 `src/index.html`。正式版已把所有必要程式放進同一個 HTML，沒有網路也能使用。

若公司電腦的安全政策阻止本機 HTML 執行或下載檔案，請改用允許本機檔案操作的瀏覽器，或請資訊人員協助。

### 2. 加入文件

你可以：

- 把一份或多份文件直接拖到虛線框內。
- 點虛線框或「選取檔案」，再從檔案選擇視窗加入。
- 混合加入圖片、PDF、Word、Excel 與 PowerPoint，不必分批處理。

加入後，工具會立即在本機讀取並掃描。大型檔案可能需要稍候，卡片上的「讀取解析中」消失後才代表掃描完成。

### 3. 看懂風險顏色

| 顯示 | 白話意思 | 建議 |
|---|---|---|
| 🔴 高風險 | 找到較可能直接洩漏位置、身分或文件內容的資料 | 對外傳送前建議清除並人工複查 |
| 🟡 中風險 | 找到作者、裝置、縮圖、隱藏內容提示等資料 | 確認是否適合讓收件者知道 |
| 🔵 低風險 | 找到較一般的附加資料，例如 XMP | 仍建議依交付需求清除 |
| 🟢 無明顯風險 | 在本工具目前會檢查的項目中，沒有找到明顯 Metadata | 不代表文件已經過完整資安或內容稽核 |
| 無法處理／已略過 | 檔案損毀、格式不符、加密或超過限制 | 先查看卡片上的原因，再處理原始檔 |

風險等級是協助判讀的提示，不是法律、合規或資料分級的最終結論。

### 4. 查看每種格式的掃描內容

#### 圖片（JPG、JPEG、PNG、WebP）

工具會查看拍攝時間、相機品牌、相機型號、GPS 座標與 XMP。GPS 會被列為高風險；拍攝時間或裝置資訊通常列為中風險；只有 XMP 時通常列為低風險。

WebP 與 JPG、PNG 使用同一套操作流程。清理後仍維持 `.webp`，不會偷偷轉成其他圖片格式；圖片尺寸及影像資料會保留。

#### PDF

工具會查看作者、建立軟體、生產者、超連結、註解／標記、表單欄位、附件及自動動作等項目。

#### Word（DOCX）

工具會查看作者、最後修改者、公司、自訂屬性、縮圖、註解、本機路徑連結與追蹤修訂。

#### Excel（XLSX）

工具會查看作者、最後修改者、公司、自訂屬性、縮圖、樞紐分析表快取、外部連結及隱藏工作表。

#### PowerPoint（PPTX）

工具會查看作者、最後修改者、公司、自訂屬性、縮圖、講者備忘稿、裁切原圖殘留及隱藏投影片。

### 5. 決定哪些文件要清除

掃描成功的文件預設會勾選「此份要清除」。畫面上方會顯示：

- 「即將清除」：按下開始後會產生清理版的數量。
- 「保留原樣」：本輪不處理的數量。

取消勾選後，該文件不會被清除，也不會放進「已清除 ZIP」。原始檔始終留在原本位置，不會被網頁刪除或覆寫。

### 6. 開始清除

按「開始清除 Metadata」。完成後，每份成功處理的文件會出現：

- 「清除前」與「清除後（驗證結果）」對照表。
- 「下載清除版」按鈕。
- 頁面上方的批次下載與複製摘要按鈕。

請查看清除後欄位是否為「✓ 已清除」。若顯示「仍在」或「無法處理」，不要直接把該產物當成已完成清理。

### 7. 下載結果

你有四種輸出方式：

1. 「下載清除版」：只下載該份文件，檔名會加上 `_cleaned`。
2. 「📦 打包下載已清除 ZIP」：把本輪所有成功清理的文件放進 `cleaned_privacy_documents.zip`。
3. 「📋 複製風險摘要」：把文件名稱與風險結果複製到剪貼簿，方便貼到交付紀錄或訊息中。
4. 「💾 下載本工具離線版」：下載乾淨的 `all-in-one-cleaner-offline.html`，可帶到斷網環境繼續使用。

離線版只包含工具本身，不會把你剛才處理的檔名、Metadata、文件內容或畫面卡片一起存進去。

## 實際會清除什麼？

| 格式 | 會自動清除 | 可能造成的影響／注意事項 |
|---|---|---|
| JPG／JPEG／PNG／WebP | EXIF、GPS、拍攝時間、裝置資訊、XMP | 保留影像資料與尺寸；仍應人工確認畫面本身沒有姓名、地址或定位資訊 |
| PDF | 文件屬性、XMP、連結／註解標記、表單、附件、自動動作 | 互動表單、可點擊連結、附件或批註可能不再存在 |
| DOCX | 作者、最後修改者、公司、自訂屬性、縮圖、註解關聯、本機路徑超連結 | 追蹤修訂只偵測、不自動接受或拒絕；畫面上可見的本機路徑文字可能仍保留 |
| XLSX | 作者、最後修改者、公司、自訂屬性、縮圖、樞紐分析表快取、外部連結 parts | 清除快取後原樞紐分析表可能失效；隱藏工作表只偵測、不自動刪除；公式中的外部參照文字可能仍需人工處理 |
| PPTX | 作者、最後修改者、公司、自訂屬性、縮圖、講者備忘稿；可安全處理時會移除裁切區外的原圖資料 | 隱藏投影片只偵測、不自動刪除；特殊或重複引用的裁切圖片應再人工檢查 |

工具不會自動改寫文件正文，因為「刪掉內容」與「清除 Metadata」是不同的工作。只偵測的項目出現時，請回到 Word、Excel 或 PowerPoint 人工確認。

## WebP 使用重點

- 可直接加入 `.webp`，不需要先轉成 JPG 或 PNG。
- 可掃描 WebP 裡的 EXIF、GPS、相機資訊與 XMP。
- 清理後仍為 `.webp`，下載類型為 `image/webp`。
- 工具會移除 WebP 的 EXIF／XMP chunks 及對應 VP8X 旗標，同時保留圖片尺寸、Alpha 等非隱私資訊。
- 損毀或冒充 WebP 的檔案會顯示「可能已損毀，或不是合法的 WEBP 格式」，不會當成乾淨圖片繼續處理。

## 隱私與安全說明

- 文件只在目前瀏覽器分頁的記憶體中處理。
- 工具不需要登入、帳號、API 金鑰或雲端服務。
- 正式版可以用 `file://` 直接開啟，處理期間不需要連網。
- 下載的是新產物，原始檔不會被覆寫。
- 按「清空佇列」可以移除目前頁面上的文件卡片與處理狀態。
- 關閉分頁後，尚未下載的處理結果不會自動保存。

若文件屬於高度機密資料，仍建議先中斷網路、使用受管理的電腦，並依組織的資料處理政策操作。

## 常見訊息與處理方式

### 「不支援的檔案格式」

確認副檔名是否在支援清單內。不要只把副檔名改成 `.pdf`、`.docx` 或 `.webp`；檔案的內部格式也必須正確。

### 「檔案超過上限，已於讀取前拒絕」

圖片、DOCX、XLSX、PPTX 單檔不可超過 20 MB；PDF 不可超過 50 MB。請先壓縮或拆分文件，再重新加入。

### 「批次檔案總量超過 100MB」

減少本次加入的檔案數量，分成數批處理。

### 「PDF 檔案解析失敗或有密碼保護」

先用 PDF 閱讀器確認檔案能正常開啟。若有密碼，請在取得合法權限後解除保護，再交給本工具。

### 「Office 檔案有密碼保護」

請先在 Word、Excel 或 PowerPoint 解除保護並另存新檔。工具不會嘗試破解密碼。

### 「檔案可能已損毀，或不是合法格式」

用原本的應用程式開啟並另存新檔，再重新掃描。只改副檔名通常無法解決問題。

### 按下載後找不到檔案

查看瀏覽器右上角的下載清單與系統「下載項目」資料夾。若瀏覽器封鎖多檔下載，請允許此本機頁面下載，或改用批次 ZIP。

## 這個工具不能代替什麼？

本工具專注於已支援格式中的隱私 Metadata，不等於完整的內容去識別化或惡意程式掃描。它不會自動：

- 遮住照片或 PDF 畫面上看得見的姓名、臉孔、地址、車牌或浮水印。
- 判斷正文、儲存格、投影片文字是否包含個資或商業機密。
- 接受或拒絕 Word 的追蹤修訂。
- 刪除 Excel 的隱藏工作表或 PowerPoint 的隱藏投影片。
- 破解加密或有密碼的文件。
- 檢查文件是否含病毒、惡意巨集，或符合特定法規。

## 建議的安全交付流程

1. 保留原始檔，只使用副本進行清理。
2. 加入工具並查看所有風險提示。
3. 對「只偵測、不自動刪除」的項目回原應用程式處理。
4. 執行 Metadata 清除並下載 `_cleaned` 版本。
5. 用原應用程式重新開啟清理版，檢查內容與功能。
6. 再把清理版放回本工具掃描，確認沒有明顯 Metadata。
7. 最後才將清理版交付給外部收件者。

## 品質驗證狀態

目前正式單一 HTML 已通過功能、清除下載、容量、效能與資訊安全五套自動化測試，涵蓋 JPG、JPEG、PNG、WebP、PDF、DOCX、XLSX、PPTX 八種副檔名。詳細證據請參閱 `qa/TEST_REPORT.md`。
