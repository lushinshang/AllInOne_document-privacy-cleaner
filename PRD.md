# PRD — All-in-One 文件隱私 Metadata 清理器

- **英文短名稱**：`all-in-one-cleaner`
- **類型**：新功能 / 整合功能
- **部署型態**：單一 HTML / 離線可用
- **AI**：否
- **SDD**：`sdd/all-in-one-cleaner/`

---

## 1. 為什麼做

在現代辦公與資訊安全場景中，對外交付文件（如合約、招標書、設計圖、個人照片等）前移除敏感 Metadata 是一項基本且關鍵的安全要求。
先前我們分別實作了 Day15 (圖片 EXIF)、Day25 (PDF 對外交付) 與 Day30 (Office OOXML) 的清理工具。然而，當使用者同時擁有多種類型的文件時，需要分開開啟多個網頁進行處理，體驗上較為破碎。

本專案旨在**將這三項功能整合進一個單一 HTML 檔案 (All-in-One)**。使用者只需將所有待清理的檔案一次性拖入網頁，即可自動識別格式、掃描各自的風險，並一鍵完成所有檔案的隱私清理與批次下載。

---

## 2. MVP 功能範圍

本工具必須在**純前端（瀏覽器端）**離線執行，所有敏感資料均不發送至任何伺服器。

### A. 多格式檔案識別與分流 (Router)
- 支援檔案格式：
  - **圖片 (Image)**：`.jpg`, `.jpeg`, `.png`, `.webp`
  - **PDF 文件**：`.pdf`
  - **Office 文件**：`.docx`, `.xlsx`, `.pptx`
- 自動根據副檔名及 MIME Type 將檔案分流至對應的處理模組。
- 單檔大小上限限制：
  - 圖片：`<= 20MB`
  - PDF：`<= 50MB`
  - Office 文件：`<= 20MB`
  - 批次總量限制：單次上傳總容量 `<=` 100MB，超過時拒絕並提示。

### B. 隱私 Metadata 風險掃描與檢視
- **圖片**：
  - 掃描並展示 EXIF 標記，如拍攝日期、相機品牌與型號、GPS 經緯度座標。
  - **高風險**：包含 GPS 座標（提供地圖預覽連結）。
  - **中風險**：包含拍攝日期或裝置型號。
  - **低風險**：包含 XMP。
- **PDF**：
  - 掃描並展示文件屬性（作者、標題、建立/修改時間、建立軟體等）、超連結、註解與註記、表單欄位、內嵌附件、動作與腳本。
  - **高風險**：包含外部/內部路徑超連結、動作與腳本、內嵌附件、註解。
  - **中風險**：包含文件屬性（Metadata）。
- **Office (docx, xlsx, pptx)**：
  - 掃描並展示文件基本屬性（作者、最後修改者、公司名稱）、自訂屬性、內嵌縮圖、註解、本機路徑連結、樞紐分析表快取、講者備忘稿等。
  - **高風險**：包含作者/修改者/公司資訊、本機路徑超連結、註解、講者備忘稿、樞紐分析快取。
  - **中風險**：包含縮圖、自訂屬性。

### C. Metadata 刪除與清理
- **圖片**：依 JPEG、PNG、WebP 容器格式清除 EXIF 與 XMP 區段／chunk，保留圖片像素、畫面尺寸及非隱私資料。
- **PDF**：重構 PDF 結構，清除 Metadata (Metadata & Info dict)、超連結標註、註解標註、表單欄位、附件與動作。
- **Office**：修改 ZIP 打包的 XML，重置 `docProps/core.xml` 和 `docProps/app.xml`、刪除 `docProps/custom.xml`、刪除內嵌縮圖、移除註解 XML、重置樞紐分析快取 XML。

### D. 清除結果與下載匯出
- **前後對照**：清除完成後，網頁會再次利用核心引擎解析清理後的檔案 Byte，輸出「清除前 vs 清除後」的數據對比，向使用者證明隱私確實已被擦除。
- **檔案下載**：
  - 支援單一檔案下載。
  - 支援多檔案批次下載（利用 `fflate` 壓縮成一個 ZIP 壓縮包）。
- **下載本工具離線版**：
  - 一鍵匯出清理後的單一 HTML 離線版本，移除所有已處理檔案的快取，讓使用者能安全保存並在完全斷網的環境下使用本工具。

---

## 3. 驗收條件

- **情境：批次上傳多種檔案並檢視風險**
  - 當使用者將包含 `photo.jpg` (有 GPS)、`contract.pdf` (有作者屬性)、`report.xlsx` (有樞紐快取) 的檔案批次拖入拖放區：
  - 網頁能正確顯示 3 個檔案，且分別顯示其對應的風險等級（高/中/低風險）。
  - 對 `photo.jpg` 顯示 GPS 地圖連結；對 `contract.pdf` 顯示作者及建立時間；對 `report.xlsx` 顯示樞紐分析快取存在。

- **情境：執行清除與對比驗證**
  - 當使用者點擊「開始清除 Metadata」：
  - 網頁會在前端以非同步方式完成擦除，並在畫面上顯示清除前與清除後的對照表。
  - 對照表應明確標示「清除後」所有敏感欄位皆為「✓ 已清除」（或無風險值）。

- **情境：無風險檔案處理**
  - 當使用者上傳一個不含任何 EXIF 的乾淨 `clean.png`：
  - 網頁不應報錯，且該檔案的風險狀態顯示為「🟢 無明顯風險（無 Metadata）」。

- **情境：批次下載與離線版**
  - 當點擊「📦 打包下載已清除 ZIP」，能正確下載包含所有已清理檔案的 ZIP 壓縮包。
  - 當點擊「💾 下載本工具離線版」，會下載一個 `.html` 檔案，在斷網情況下用瀏覽器雙擊打開，依然能重複執行上述所有解析與清理動作。
